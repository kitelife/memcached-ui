package memcached

/*
import (
	"testing"
)

func Test_Send(t *testing.T) {
	c := Connection{
		Host: "127.0.0.1",
		Port: 11211,
	}
	cmd := "stats\r\n"
	resp, err := c.Send(cmd)
	if err != nil {
		t.Error(err)
	} else {
		t.Log(string(resp))
	}
	c.Conn.Close()
}
*/
